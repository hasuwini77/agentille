# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Logs
*.log
# agentille's per-run shipped log is a runtime artifact, not source
docs/agentille-log.md

# Env / secrets (paranoia — there should be no env files in a markdown repo)
.env
.env.*

# Node (in case anyone adds tooling later)
node_modules/
dist/
build/

# Local Claude Code state
.claude/

# Personal / local Claude instructions (the committed CLAUDE.md is the shared one)
CLAUDE.local.md

# Planning & working docs — local-only, never committed.
# Anything that references private or external context must live here (or
# outside the repo in ~/.agentille/), never in tracked files. See CLAUDE.md.
docs/superpowers/
docs/agentille/

# Playwright MCP / QA review artifacts (screenshots saved to the working dir)
.playwright-mcp/
*-light.png
*-dark.png
review-*.png
