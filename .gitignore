# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Logs
*.log
# agentille's per-run shipped log is a runtime artifact, not source
docs/agentille-log.md

# Env / secrets (paranoia — there should be no env files in a markdown repo)
.env
.env.*

# Node (in case anyone adds tooling later)
node_modules/
dist/
build/

# Local Claude Code state
.claude/
